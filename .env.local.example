# Local SQLite (default — no setup required)
# DB_DRIVER=sqlite
# SQLITE_PATH=./data/cricket-score.db

# Neon Postgres (production / optional local)
# DB_DRIVER=postgres
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
